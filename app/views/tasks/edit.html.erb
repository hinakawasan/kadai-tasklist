<h1>タスクを編集する</h1>
<%= form_with(model: @message, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  <%= f.submit '追加' %>
<% end %>
<%= link_to '一覧に戻る', tasks_path %>